{
  "version": 2,
  "buildCommand": "npm install --registry=${NPM_REGISTRY} && npm run build",
  "installCommand": "npm install --registry=${NPM_REGISTRY}",
  "framework": "nextjs",
  "regions": ["iad1"]
} 